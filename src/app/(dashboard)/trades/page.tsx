import { TradesList } from "@/screens";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <TradesList />
    </div>
  );
};

export default page;
