import { UserCreate } from "@/screens";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <UserCreate />
    </div>
  );
};

export default page;
